# STATUS — WithGod

> 이 저장소의 **지금 상태 + 앞으로 할 일**. 세션이 바뀌어도 여기만 읽으면 이어서 작업할 수 있다.
> **작업을 완료하면 「지금 상태」와 「최근 변경 이력」을 갱신할 것.** 낡으면 신뢰가 무너진다.
> ⚠️ 이 파일은 세션 시작 시 통째로 주입된다. **20KB 아래로 유지할 것** —
> 완료 이력은 `CHANGELOG.md` 나 git history 로 넘긴다.
> 최종 갱신: 2026-09-02 · 브랜치: `app-develop`

## 한 줄

성경 말씀 앱. 모노레포 — `WithGod/`(React Native·Expo) · `backend/`(FastAPI, 푸시 스케줄러) · `withgod-web/`(웹·어드민 대시보드).

## 지금 상태

**오늘의 말씀 목록을 처음부터 다시 만들고 있다.** 385개를 전수 감사했더니 163개(42%)가
부적합이었다 — 61권 균등 배분 때문에 룻기·에스라에서 바닥을 긁었고, 시편은 26개뿐이었다.
성경 전체를 다시 읽어 **793개**까지 모았고(시편·잠언·이사야·복음서·바울서신·일반서신),
지금은 이 793개를 재검토하는 중이다.

⚠️ **작업 문서는 [backend/docs/오늘의말씀_재선정.md](../backend/docs/오늘의말씀_재선정.md).**
확정된 선정 기준·파이프라인·다음 할 일이 전부 거기 있다. 이어서 작업하려면 그걸 먼저 읽을 것.

그 전에는 사이드바 스와이프를 화면 전역으로 넓히고(ChatGPT·Claude 앱과 같은 동작)
검증 중 나온 결함 둘(#28 dim 탭 미작동, #29 엣지 스와이프 오종료)을 고쳐 production
채널로 OTA 배포까지 마쳤다(runtime 1.5.0, iOS+Android, update group `048b57dc`).
iOS 는 1.5.0 이 App Store 공개 상태(2026-08-11 릴리스).

## 진행/예정

- [ ] **오늘의 말씀 재선정** — 793개 재검토 중(200/793). 표본 검증에서 32.5%가 걸려
      기준을 다시 좁히는 중이다. 재검토 → 2차 표본 검증(3개 이하 목표) →
      남은 구약 5,190절(23권) → `daily_verses.json` 교체 순.
      함께 하기로 한 것: 명절 고정 배치 · 날짜 편중 해소 · 풀이 프롬프트 개선 · 원문 조판 수정
- [ ] `data/langs/unsuitable_refs.json`(이슈 #11 분류)이 명작을 오탐한다 —
      마태복음 6:34·시편 71:20·잠언 18:21이 "위로 부적합"으로 걸러져 있고
      **검색·추천이 이 파일을 쓰고 있다.** 별건으로 볼 것
- [ ] `verses.csv` 본문 결함 — 인접 절 중복 14건, 오타(시편 117:1 「여호아」,
      히브리서 12:14 「좆으라」), 따옴표 잔여 11%·공백 8.7%. 원본 usfm 문제
- [ ] 번역본 검토 — 개역한글(1961) 어휘가 카드 이해를 막는다(793개 중 22%).
      「권고」眷顧를 勸告로 읽는 동음이의 오독이 특히 나쁘다. 현대 번역본은 저작권 협의 필요

## 앞으로 할 일 (우선순위)

> 원본은 GitHub 이슈다. 내용을 여기 복사하지 말고 `gh issue view <번호>` 로 확인한다.
> 새 버그·기능은 이슈로 등록(`bug` / `enhancement`), 운영 잡일은 이슈로 만들지 않는다.

### P1 — 푸시 신뢰성 (한 파이프라인에 얽혀 있어 같이 본다)

- #19 푸시가 앱 언어 설정을 안 따르고 영어로 옴
- #23 디바이스 upsert 가 행 전체를 덮어써 language·알림시각 리셋
- #24 무효 FCM 토큰 판별이 문자열 매칭이라 죽은 토큰이 안 지워짐
- #25 알림 시각이 지난 뒤 등록한 기기에 즉시 푸시 1회 발송

### P2 — 앱 결함 · 배포 자동화

- #16 위젯 탭 시 앱이 재시작되는 것처럼 스플래시 재생
- #17 대시보드 로그인에서 Enter 키로 제출 안 됨
- #18 최신버전 값이 1.2.4 로 방치 — 배포 시 자동 갱신 필요
- #27 R8 릴리스 빌드 검증 (안드로이드 홈 위젯 중점)

### P3 — 확장

- #8 / #9 다국어 지원(i18n) — 문자열 추출·8개 언어 번역
- #26 대형 화면(태블릿·폴더블) 대응

⚠️ 위 우선순위는 라벨과 성격으로 정리한 **제안**이다. 실제와 다르면 고칠 것.

## 알려진 한계

- Play 프로덕션 신청과 연령등급 질문이 남아 있다.

## 최근 변경 이력

- 2026-09-02 wip(backend): 오늘의 말씀 재선정 — 385개 감사(42% 부적합) 후 전면 재작업, 793개까지 수집
- 2026-08-21 fix(app): dim 탭 미작동(#28)·엣지 스와이프 오종료(#29) 수정 → OTA 배포 완료
- 2026-08-21 feat(app): 사이드바 스와이프를 화면 전역으로 확대 → OTA 배포 완료 (iOS·안드로이드 검증)
- 2026-08-19 fix(backend,web): 언어 카드 모집단을 국가 분포와 맞춤
- 2026-08-18 feat(backend,web): 대시보드에 국가별 전환·언어 지표 추가
- 2026-08-18 feat(backend): 오늘의 말씀 7개 → 385개 (1년 무반복)
- 2026-08-11 fix(app): 사이드바 상단바를 앱 공용 헤더 규격에 맞춤
- 2026-08-11 fix(app): 메뉴 버튼이 안 눌리던 문제 + 드로어 닫힘 감속
- 2026-08-10 fix(app): 좌측 엣지 스와이프를 ScrollView 에 뺏기던 문제
