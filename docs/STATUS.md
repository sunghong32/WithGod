# STATUS — WithGod

> 이 저장소의 **지금 상태 + 앞으로 할 일**. 세션이 바뀌어도 여기만 읽으면 이어서 작업할 수 있다.
> **작업을 완료하면 「지금 상태」와 「최근 변경 이력」을 갱신할 것.** 낡으면 신뢰가 무너진다.
> ⚠️ 이 파일은 세션 시작 시 통째로 주입된다. **20KB 아래로 유지할 것** —
> 완료 이력은 `CHANGELOG.md` 나 git history 로 넘긴다.
> 최종 갱신: 2026-08-21 · 브랜치: `app-develop`

## 한 줄

성경 말씀 앱. 모노레포 — `WithGod/`(React Native·Expo) · `backend/`(FastAPI, 푸시 스케줄러) · `withgod-web/`(웹·어드민 대시보드).

## 지금 상태

사이드바 스와이프를 화면 전역으로 넓혔다(ChatGPT·Claude 앱과 같은 동작). 좌측 엣지
밖에서는 가로 12dp·세로의 2배 이상일 때만 드로어가 받아 세로 스크롤을 뺏지 않는다.
iOS 시뮬레이터·안드로이드 에뮬레이터 양쪽에서 열기·닫기·☰ 버튼·세로 스크롤을
확인하고 **production 채널로 OTA 배포까지 마쳤다**(runtime 1.5.0, iOS+Android).

이어서 검증 중 나온 결함 둘을 고쳤다(#28 덮인 화면 탭이 안 먹던 문제, #29 엣지
스와이프가 시스템 뒤로가기로 넘어가 앱이 즉시 종료되던 문제 — 홈에서 뒤로가기는
두 번 눌러야 종료). **이 수정분은 아직 OTA 로 내보내지 않았다** — 현재 라이브 OTA는
#28 이 남아 있는 상태다.

그 전에는 어드민 대시보드 지표 확장을 마친 참이었다. 국가별 전환·언어 지표를 추가했고, 언어 카드
모집단을 국가 분포와 맞추는 수정까지 반영됐다. 오늘의 말씀은 7개 → 385개로 늘려
1년 무반복이 된다. iOS 는 1.5.0 이 App Store 공개 상태(2026-08-11 릴리스).

**(자동 생성된 초안 — 다음 세션에서 실제 상태로 갱신할 것)**

## 진행/예정

- [ ] (없으면 「없음」이라고 적는다)

## 앞으로 할 일 (우선순위)

> 원본은 GitHub 이슈다. 내용을 여기 복사하지 말고 `gh issue view <번호>` 로 확인한다.
> 새 버그·기능은 이슈로 등록(`bug` / `enhancement`), 운영 잡일은 이슈로 만들지 않는다.

### P1 — 푸시 신뢰성 (한 파이프라인에 얽혀 있어 같이 본다)

- #19 푸시가 앱 언어 설정을 안 따르고 영어로 옴
- #23 디바이스 upsert 가 행 전체를 덮어써 language·알림시각 리셋
- #24 무효 FCM 토큰 판별이 문자열 매칭이라 죽은 토큰이 안 지워짐
- #25 알림 시각이 지난 뒤 등록한 기기에 즉시 푸시 1회 발송

### P2 — 앱 결함 · 배포 자동화

- #16 위젯 탭 시 앱이 재시작되는 것처럼 스플래시 재생
- #17 대시보드 로그인에서 Enter 키로 제출 안 됨
- #18 최신버전 값이 1.2.4 로 방치 — 배포 시 자동 갱신 필요
- #27 R8 릴리스 빌드 검증 (안드로이드 홈 위젯 중점)

### P3 — 확장

- #8 / #9 다국어 지원(i18n) — 문자열 추출·8개 언어 번역
- #26 대형 화면(태블릿·폴더블) 대응

⚠️ 위 우선순위는 라벨과 성격으로 정리한 **제안**이다. 실제와 다르면 고칠 것.

## 알려진 한계

- Play 프로덕션 신청과 연령등급 질문이 남아 있다.

## 최근 변경 이력

- 2026-08-21 fix(app): dim 탭 미작동(#28)·엣지 스와이프 오종료(#29) 수정 — OTA 미배포
- 2026-08-21 feat(app): 사이드바 스와이프를 화면 전역으로 확대 → OTA 배포 완료 (iOS·안드로이드 검증)
- 2026-08-19 fix(backend,web): 언어 카드 모집단을 국가 분포와 맞춤
- 2026-08-18 feat(backend,web): 대시보드에 국가별 전환·언어 지표 추가
- 2026-08-18 feat(backend): 오늘의 말씀 7개 → 385개 (1년 무반복)
- 2026-08-11 fix(app): 사이드바 상단바를 앱 공용 헤더 규격에 맞춤
- 2026-08-11 fix(app): 메뉴 버튼이 안 눌리던 문제 + 드로어 닫힘 감속
- 2026-08-10 fix(app): 좌측 엣지 스와이프를 ScrollView 에 뺏기던 문제
