# 오늘의 말씀 재선정 — 작업 문서

> 진행 중인 대형 작업. 새 세션은 **이 문서를 먼저 읽고** 「다음 할 일」부터 이어간다.
> 최종 갱신: 2026-09-02

## 왜 하는가

`notifications/daily_verses.json` 의 385개 목록을 전수 감사한 결과 **163개(42%)가 오늘의
말씀으로 부적합**했다(감사표: `docs/daily_verses_audit.csv`).

근본 원인은 **61권 균등 배분(라운드로빈)**이다. 명작 밀도는 책마다 다른데 균등하게
뽑으라고 하니 룻기·에스라·역대하에서 바닥을 긁었다 — 보아스가 추수꾼에게 건넨 인사
(룻 2:4), 아사 왕의 축성 기록(대하 14:6), 고레스 칙령(스 1:3). 반면 시편은 26개(6.8%)뿐.

부수 결함: 본문 중복 6쌍, `index = 날짜 % 385` 라 품질이 달력에 고정(크리스마스=축도,
새해=문장 조각), 잘 만들어진 `_POPULAR_VERSE_REFS` 170개는 `/random` 이 사문화돼 방치.

**AI 풀이가 부실한 것도 여기서 온다.** 구절 자체에 독립된 메시지가 없으면 풀이도 붕 뜬다.

## 확정된 기준 (사용자 결정 — 임의로 바꾸지 말 것)

**핵심 질문: 읽고 바로 와닿고 이해되는가?**

- **위안** — 지금 아픈 사람에게 건네는 말 / **교훈** — 살아가는 데 도움이 되는 통찰
- **찬양·신앙고백·교리 서술은 제외.** 「여호와는 나의 X시니」 형태의 정체 서술은
  아무리 유명해도 뺀다 — **시편 23:1, 요한복음 3:16도 제외**했다.
- 앱 사용자는 **대부분 기독교인**이다. "비신자도 읽히는가"가 기준이 아니다.
  「외인」(교회 밖 사람) 같은 표현은 문제되지 않는다.
- **목록 개수는 목표가 아니라 결과값이다.** 반복 주기는 신경 쓰지 않는다.
- **길이 하한 없음**(`MIN_LEN = 0`). 좋은 문장은 저절로 길어지고, 짧아도 심금을
  울리면 좋은 문장이다 — 「안심하라 내니 두려워 말라」(15자).
- **합본 허용** — 문장이 끊길 때만. 길이를 채우려고 붙이지 않는다(시편 119:105가
  106절에 희석된 사고가 있었다). 표기는 「시편 40:1-2」.
- **기도문 포함** — 「…하소서」로 끝나는 간구도 좋은 것은 넣는다.
- **서사 껍데기 제거(B 방식) 허용** — 절 안의 "예수께서 가라사대 … 하시니라" 같은
  서술만 벗기고 말씀은 한 글자도 안 고친다. 표준 표기 `b`를 붙인다(요한복음 8:11b).
  **어미까지 다듬는 것(C안)은 하지 않는다.**
- **제외 대상 추가분**: 비하 표현(소경·벙어리·문둥이·병신 등), 가부장적 가정 규범
  (엡 5:22, 골 3:18, 딤전 2:11-12), 종·상전 구절(엡 6:5, 골 3:22, 딛 2:9).

## 파이프라인

```
data/verses.csv (31,084절)
  └─ scripts/shortlist_daily_verses.py     배제 규칙만으로 후보 압축 → 12,600절
       · 점수를 매기지 않는다. 이전 실패가 '위로 키워드 점수'에서 왔다.
       · --check 로 손큐레이션 170개 회수율 측정(현재 89%)
       └─ docs/daily_verse_candidates.csv
            └─ (사람이 읽고 고름) → docs/verse_picks/*.tsv   [ref, type, note]
                 └─ scripts/build_verse_cards.py   본문 결합·서사 제거·완결성 검사
                      └─ docs/verse_picks/*_cards.csv  [reference, type, merged, len, text, note]
                           └─ scripts/recheck_verse_cards.py  재검토 판정 기록·반영
                                └─ docs/verse_picks/recheck.tsv  [reference, verdict, reason]
```

두 스크립트 모두 **`languages.py`·`app.py` 소스를 파싱**해서 책 코드와 손큐레이션
목록을 가져온다(fastapi 의존성을 피하려고 import 대신 파싱).

## 현재 상태 (793개)

| 파일 | 개수 | 범위 |
|---|---|---|
| `psalms_cards.csv` | 122 | 시편 |
| `proverbs_cards.csv` | 206 | 잠언·전도서 |
| `isaiah_cards.csv` | 89 | 이사야·예레미야애가·소선지서 |
| `gospels_cards.csv` | 150 | 복음서 4권 |
| `paul_cards.csv` | 160 | 바울서신 13권 |
| `general_cards.csv` | 66 | 일반서신·히브리서·요한계시록 |
| **계** | **793** | 위안 298 · 교훈 495 |

## 다음 할 일 (순서대로)

### 1. 793개 전수 재검토 — **진행 중, 200/793 완료 (탈락 52)**

표본 40개 검증에서 **13개(32.5%)가 걸렸다.** 기준이 어긋난 것이므로 남은 구약을
읽기 전에 이걸 먼저 끝내야 한다.

**진행분은 `docs/verse_picks/recheck.tsv` 에 저장된다. 세션이 끊겨도 이어서 하면 된다.**
`scripts/recheck_verse_cards.py` 로 다룬다.

```bash
python scripts/recheck_verse_cards.py status                 # 어디까지 했나
python scripts/recheck_verse_cards.py next --limit 200        # 아직 안 본 것만 보여준다
# ↑ 읽고 판정한 뒤 ↓ 탈락만 적는다. 나머지는 --keep-rest 로 한 번에 통과 처리
python scripts/recheck_verse_cards.py mark \
    --drop "마태복음 5:23" "누가복음 10:20" --reason "끊김"
python scripts/recheck_verse_cards.py mark --keep-rest 200
python scripts/recheck_verse_cards.py apply                   # 전부 끝나면 *.tsv 에 반영
```

`apply` 는 drop 판정을 선정 목록에서 제거하기만 한다. **그 뒤 `build_verse_cards.py`
를 다시 돌려야 카드가 갱신된다**(명령은 apply 출력이 알려준다).

**200개까지 읽은 결과 52개(26%)가 탈락.** 탈락 유형:

| 유형 | 예 |
|---|---|
| 끊김(합본 실패) | 마태 5:23「…생각나거든」 · 마태 25:35「…영접하였고」 · 마태 20:26「…되고」 |
| 문맥 지시어 | 요이 1:6「사랑은 **이것**이니」 · 눅 10:42「마리아는 **이 좋은 편**을」 · 딤후 2:7「**내 말**하는 것을」 |
| 난해 어휘 | 히 12:11「연달한 자」 · 벧전 3:8「체휼하며」 · 약 3:17「편벽」 |
| 성경 인물 지식 | 약 5:11「**욥**의 인내」 |
| 화자 개인 사정 | 엡 3:13「너희를 위한 **나의** 여러 환난」 |
| 이중부정·반문으로 흐림 | 시편 22:24 · 마태 6:27「키를 한 자」 |
| 책망·무거움 | 마 12:34「독사의 자식들아」 · 마 18:33-34「옥졸들에게 붙이니라」 |
| 중복 | 마 15:11 ↔ 15:18 · 벧전 4:9-10 ↔ 4:10 |

기계로 잡히는 건 85개(11%)뿐이고 **나머지는 사람이 읽어야 한다.**

### 2. 두 번째 표본 40개로 재검증

**걸리는 게 3개 이하**로 떨어져야 다음 단계로 넘어간다. 그 전에는 남은 구약을
읽지 않는다 — 기준이 틀린 채로 읽으면 두 번 일하게 된다.

### 3. 남은 구약 5,190절 읽기 (23권)

욥기 578 · 에스겔 557 · 예레미야 546 · 창세기 436 · 신명기 392 · 출애굽기 389 ·
사도행전 340 · 민수기 265 · 레위기 219 · 역대하 167 · 열왕기상 161 · 사무엘하 150 ·
느헤미야 127 · 역대상 120 · 다니엘 119 · 사사기 114 · 여호수아 102 · 사무엘상 98 ·
아가 97 · 열왕기하 95 · 에스라 59 · 에스더 45 · 룻기 14

뒤로 갈수록 밀도가 급락한다(레위기·민수기·역대기는 거의 안 나올 것).

### 4. 최종 목록 반영 + 승인된 부수 작업

- `notifications/daily_verses.json` 교체
- **명절·절기 고정 배치** — 지금은 크리스마스=고린도후서 13:13(축도),
  새해 첫날=에베소서 1:3(문장 조각), 설날=유다서 1:2(인사말)
- **날짜 편중 해소** — `index = 날짜 % N` 이라 배열 순서가 곧 달력이다.
  좋은 구절이 특정 시기에 몰리지 않게 분산
- **풀이 프롬프트 개선** — `app.py:515` `_VERSE_INTERPRETATION_SYSTEM`
- **원문 조판 수정** — 아래 참조

## 작업 중 발견한 결함 (별건)

### 본문 데이터

- **`verses.csv` 전사 오류** — 인접 절 본문이 동일한 곳 14건(시편 3:5=3:6,
  시편 10:12가 16:11 본문). 원본 `data_raw/kor_usfm.zip` 자체 문제로 확인.
  `scripts/repair_ko_verses.py`(이슈 #13)는 절 번호 집합만 비교해서 이 유형을 못 잡는다.
- **오타** — 시편 117:1「여호아」, 히브리서 12:14「좆으라」, 시편 90:4「뿐 임이니이다」,
  데살로니가후서 1:11「믿 음」, 다니엘 6:27「땅에서든지이적」
- **조판 잡티** — 따옴표 잔여 3,406절(11%), 「하리라 !」 공백 2,691절(8.7%).
  `_keep_korean_only`(`app.py:125`)가 한자·가나만 지우고 이건 안 건드린다.
  오늘의 말씀은 빌드 때 정리되지만 **추천·검색 경로는 그대로 노출될 가능성**이 있다.

### 기존 필터

- **`data/langs/unsuitable_refs.json`(이슈 #11, gpt-4o-mini 분류)이 명작을 오탐한다.**
  마태복음 6:34「내일 일을 위하여 염려하지 말라」, 시편 71:20, 잠언 18:21이
  "위로 부적합"으로 걸러져 있다. **이 파일은 검색·추천 기능이 지금 쓰고 있다.**

### 번역본 자체

`app.py:3` 에 `KR1910, Public Domain`. 저작권 때문에 1961년 개역한글을 쓰는데
**두 문제가 여기서 나온다.**

- **어휘** — 793개 중 123개(22%)에 뜻이 바로 안 잡히는 한자어. 「독생자」(외아들),
  「경외」16회, 「인자」14회, 「긍휼」12회
- **동음이의 오독** — 뜻이 통하는 것처럼 보이면서 틀리게 읽힌다.
  「권고」眷顧(돌보심)를 勸告(충고)로, 「은사」恩賜(선물)를 恩師(스승)로.
  베드로전서 5:7이 이 때문에 탈락
- **옛 맞춤법** — 「일찌기」(일찍이), 「~찌어다」(~지어다)

현대 번역본(개역개정·새번역)이면 대부분 해소되지만 **대한성서공회 사용 허락과
비용**이 필요하다. 오늘의 말씀뿐 아니라 검색·추천·위젯 전체에 걸리는 문제라
이 작업에 끼워 넣을 크기가 아니다. **별건으로 검토할 것.**

## 이번에 고친 빌더 버그 (재발 주의)

- **어미 훼손** — `\s*하니라$` 규칙이 공백 없이도 매칭돼 「그 날에 족**하니라**」를
  「…족」으로 잘랐다. 공백을 반드시 요구(`\s+`)하도록 수정. 4개 복구.
- **합본 ↔ 하반절 충돌** — 껍데기를 벗긴 절에 뒤 절을 붙이면 그 절의 서사가 도로
  딸려 온다(요한 8:7 + 8:8 = 「…돌로 치라! 하시고 다시 몸을 굽히사」).
  `partial` 이면 합본하지 않도록 수정.
- **끊김 판정 누락** — 「…노니」(빌 4:11), 「…것이니」(마 7:7)가 완결로 통과했다.
  반대로 팔복의 「…것임이요」는 완결로 둬야 해서 `(?<!임)이요` 로 구분.

## 참고 링크

- 385개 전수 감사표 — `docs/daily_verses_audit.csv`
- 표본 40 검증판 — https://claude.ai/code/artifact/434f1436-7af0-4567-a512-092d50e67eeb
- 선정 기준 정리판 — https://claude.ai/code/artifact/4619ada3-2df5-40f0-9978-17a62d302410
